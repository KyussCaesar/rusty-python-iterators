# Rusty iterators for Python

This library provides the `Iterator` type, which provides many of the same
methods presented by the `Iterator` trait in Rust.

See the generated documentation in `build/` for examples.

